server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/deliverydb
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
